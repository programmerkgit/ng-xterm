import { XtermDirective } from './xterm.directive';

describe('XtermDirective', () => {
  it('should create an instance', () => {
    const directive = new XtermDirective();
    expect(directive).toBeTruthy();
  });
});
